"use strict";

const Database = require("./database");

const db = new Database(options);

//functions

async function getAll() {
    try{
        const result = 

    }
}


//main function
async function run(){
    await getAll();
    await getAll();
}